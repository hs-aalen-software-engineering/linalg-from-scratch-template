{
  "name": "linalg-from-scratch",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "charliermarsh.ruff",
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "twxs.cmake",
        "tamasfe.even-better-toml"
      ],
      "settings": {
        "cmake.configureOnOpen": false,
        "cmake.buildDirectory": "${workspaceFolder}/cpp/build"
      }
    }
  },
  "postCreateCommand": "bash -lc 'cd python && uv sync || true; cd ../cpp && cmake -B build -S . || true'",
  "remoteUser": "vscode"
}
